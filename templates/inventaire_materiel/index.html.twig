{% extends 'base.html.twig' %}

{% block title %}InventaireMateriel index{% endblock %}

{% block content %}
    <h1>InventaireMateriel index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Description</th>
                <th>Numero Cartouche</th>
                <th>Ip</th>
                <th>Localisation</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for inventaire_materiel in inventaire_materiels %}
            <tr>
                <td>{{ inventaire_materiel.id }}</td>
                <td>{{ inventaire_materiel.description }}</td>
                <td>{{ inventaire_materiel.numeroCartouche }}</td>
                <td>{{ inventaire_materiel.ip }}</td>
                <td>{{ inventaire_materiel.localisation }}</td>
                <td>
                    <a href="{{ path('app_inventaire_materiel_show', {'id': inventaire_materiel.id}) }}">show</a>
                    <a href="{{ path('app_inventaire_materiel_edit', {'id': inventaire_materiel.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_inventaire_materiel_new') }}">Create new</a>
{% endblock %}
