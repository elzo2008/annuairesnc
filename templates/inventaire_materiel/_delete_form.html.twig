<form method="post" action="{{ path('app_inventaire_materiel_delete', {'id': inventaire_materiel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ inventaire_materiel.id) }}">
    <button class="btn">Delete</button>
</form>
