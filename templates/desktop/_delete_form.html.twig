<form method="post" action="{{ path('app_desktop_delete', {'id': desktop.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ desktop.id) }}">
    <button class="btn">Delete</button>
</form>
