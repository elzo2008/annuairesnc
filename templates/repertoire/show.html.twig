{% extends 'base.html.twig' %}

{% block title %}Repertoire{% endblock %}

{% block body %}
    <h1>Repertoire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ repertoire.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ repertoire.numero }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ repertoire.nom }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ repertoire.mail }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_repertoire_index') }}">back to list</a>

    <a href="{{ path('app_repertoire_edit', {'id': repertoire.id}) }}">edit</a>

    {{ include('repertoire/_delete_form.html.twig') }}
{% endblock %}
