<form method="post" action="{{ path('app_flybox_delete', {'id': flybox.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ flybox.id) }}">
    <button class="btn">Delete</button>
</form>
