{% extends 'base.html.twig' %}

{% block title %}Transfert{% endblock %}

{% block content %}
    <h1>Transfert</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ transfert.id }}</td>
            </tr>
            <tr>
                <th>Destinataire</th>
                <td>{{ transfert.destinataire }}</td>
            </tr>
            <tr>
                <th>DateTransfert</th>
                <td>{{ transfert.dateTransfert ? transfert.dateTransfert|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ transfert.commentaire }}</td>
            </tr>
            <tr>
                <th>Qte</th>
                <td>{{ transfert.qte }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_transfert_index') }}">back to list</a>

    <a href="{{ path('app_transfert_edit', {'id': transfert.id}) }}">edit</a>

    {{ include('transfert/_delete_form.html.twig') }}
{% endblock %}
